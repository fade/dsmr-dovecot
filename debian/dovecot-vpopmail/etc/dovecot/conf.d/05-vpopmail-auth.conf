# vpopmail SQL authentication — loaded before 10-auth.conf so the SQL passdb
# is tried first.  For vpopmail users (full email addresses) the SQL passdb
# matches and system passdb is never consulted.
auth_mechanisms = plain login

!include vpopmail-auth-sql.conf.ext
